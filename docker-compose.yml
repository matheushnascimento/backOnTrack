services:
  web:
    build: .
    container_name: backontrack-web
    ports:
      - "8081:8081"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      - BROWSER=none
    restart: unless-stopped
    stdin_open: true
    tty: true

volumes:
  node_modules:
